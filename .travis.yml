
services:
  - docker

before_install:
- docker pull dazz111/ubuntu-sdk
- docker run -t -d --name sdk dazz111/ubuntu-sdk  /bin/bash
- docker ps -a
- docker cp /scripts/build.sh sdk:/root/
- docker exec sdk -d -it /bin/bash -c “sh root/thread_dump.sh"
script: 
    - chmod ugo+x scripts/build.sh
    
