
services:
  - docker

before_install:
- docker pull dazz111/ubuntu-sdk:build
- docker run -t -d --name sdk dazz111/ubuntu-sdk  /bin/bash /scripts/build.sh
- docker ps -a

- docker exec sdk -d -it /bin/bash -c “bash root/build.sh"
script: 
    - chmod ugo+x scripts/build.sh
    
