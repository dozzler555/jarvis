services:
  - docker
before_install:
  - docker pull dazz111/ubuntu-sdk:latest
  - docker run -i -t 253b47eba801 /bin/bash
  - docker images
  - docker ps
  - docker container ls
  - docker exec $(docker ps -l -q) build.sh /bin/bash
